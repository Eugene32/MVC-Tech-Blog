<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TECH BLOG</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>

<body>
    <header class="fixed-top navbar pb-1 pt-1 display-flex justify-space-between align-center text-center p-2 p-3 mb-2 bg-primary text-white">
        <h1 class="fw-bold">
            TECH BLOG
        </h1>

        <nav class="">
            <a class="text-white" id="homepage" href="/">Home</a>
            <a class="text-white" id="dashboard" href="/dashboard">Dashboard</a>

            <!--Display the logout button if logged in -->
            {{#if logged_in}}
            <a href='' class=" text-white" id="logout">Logout</a>
            {{else}}
            <!--Else display the login button -->
            <a class="text-white" id="login" href="/login">Login</a>
            {{/if}}
        </nav>
    </header>
    <main class="container container-fluid mt-5 position-relative pt-5">
        {{{ body }}}
    </main>

    <div class="position-relative">
        <footer id="footer" class="position-relative">
            <h4 class="position-relative text-center"> &copy &#x1d19&#670 2021 </h4>
            <!-- Prints a copyright icon, and a reverse R and K -->
        </footer>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Script to be loaded during successful login -->
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{else}}
    <script src="/public/js/login.js"></script>
    {{/if}}

</body>

</html>